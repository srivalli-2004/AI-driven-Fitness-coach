# Leave unset for local development (defaults to http://localhost:5000/api)
# After deploying the backend, set this to your live backend URL + /api
VITE_API_URL=https://your-backend-url.onrender.com/api
